测试用例说明（简版）

测试目录：
examples/test_cases

本地测试脚本：
tests/local_validation_tests.py

一键运行：
run_local_tests.bat

或手动运行：
python tests\local_validation_tests.py
python -m py_compile codes\validators.py codes\decomposer_agent.py codes\generator_agent.py codes\qc_agent.py codes\run_pipeline.py tests\local_validation_tests.py


已覆盖角度：

1. 普通编号题
文件：case_01_flat_numbered.md
目的：检查 1. 2. 3. 这种普通编号能否正确识别。

2. 大题下多小题
文件：case_02_nested_subquestions.md
目的：检查 (1)(2)(3) 是否按独立题目计数。

3. 标题加下一行应用题
文件：case_03_title_plus_application.md
目的：检查“应用题标题 + 下一行题干”不会被漏数。

4. 变式题
文件：case_04_variants.md
目的：检查“变式 1、变式 2”等是否按独立题目计数。

5. 混合结构
文件：case_05_mixed_sections.md
目的：同时覆盖章节、大题、小题、应用题、分式题、变式题。

6. 普通文本编号
文件：case_06_no_markdown_plain.md
目的：检查不使用加粗 Markdown 时也能识别。

7. 空格与全角括号
文件：case_07_edge_spacing.md
目的：检查 **（1）**、多余空格、全角括号等格式。


额外回归测试：

第 6 题：
测试含参二次不等式 (k-2)x^2+4x+1>=0。
覆盖 k<2、k=2、2<k<6、k>=6。
用代值方式检查解集方向。

第 11 题：
测试含参分式不等式 (3-x)/(5x-n)<=1/5。
覆盖 n<15、n=15、n>15。
检查分母排除点和临界点顺序。

QC 逻辑：
测试只要 QC 输出 major 问题，就不能通过。
防止再次出现“有严重错误但 is_passed=true”的矛盾。

最终输出健康检查：
检查 outputs/generated_questions_final.md 和 outputs/answer_key_final.md 是否仍为 15 题、15 答案。
检查第 6、11 题答案块没有问号乱码。
检查 qc_final.json 为通过状态。


当前测试结果：
本地测试通过。
语法编译通过。
